# Offline documentation claims fixture

Use only as synthetic evidence. The fenced object is machine-readable test data.

```json
{
  "schema": "optiak-docs-claims-fixture/v1",
  "claims": [
    {
      "url": "https://docs.optiak.dev/getting-started/overview",
      "claim": "Application API keys authenticate gateway traffic for one application",
      "authorityConnected": false,
      "expectedClassification": "blocked_on_authority"
    }
  ]
}
```
