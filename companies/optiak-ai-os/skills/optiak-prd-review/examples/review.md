# Example review

PRD revision: `sample-1`.

Verdict: changes required.

Blocking gaps:

- It names “users” but does not distinguish organization admin from member.
- Credential lifecycle and one-time secret display are unspecified.
- No expected error behavior exists for an unavailable upstream provider.
- “Faster” has no measurable baseline or target.

Next owner: Product & PRD Lead. Architecture review starts only after these decisions are explicit.
